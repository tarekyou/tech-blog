<article class="post">
  <div class="meta , postdiv">
    <p>{{title}} by {{user.username}} on {{format_date created_at}}</p>
    
    <a href="/post/{{id}}">{{title}}</a>
  </div>
  <p class="content">{{content}} </p>
</article>